{
  "name": "ithaca-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static builder for a personalized 75-day training block. Everything runs in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
